#include <bits/stdc++.h>
using namespace std;

#define fastIO                                                                 \
  ios_base::sync_with_stdio(0);                                                \
  cin.tie(0);                                                                  \
  cout.tie(0);

int a, b, d1 = 0, d2 = 0, d = 0;
long k, m, n, t = 0;

int main() {
  fastIO cin >> a >> k >> b >> m >> n;
  while (t < n) {
    }
  cout << d;
}